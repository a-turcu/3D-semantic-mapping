#!/usr/bin/env python3

# Demo interactive agent for active control mode that performs the action
# specified by a user at runtime.  Observations are visualised by the agent at
# the completion of each action.

from benchbot_api import BenchBot
from interactive_agent import InteractiveAgent

if __name__ == '__main__':
    BenchBot(agent=InteractiveAgent()).run()
